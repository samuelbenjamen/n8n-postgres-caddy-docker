# -----------------------------------------------------------------------------
# n8n/PostgreSQL Configuration
# -----------------------------------------------------------------------------

# PostgreSQL Credentials (CHANGE THESE!)
POSTGRES_USER=n8n_user
POSTGRES_PASSWORD=secure_random_password_12345
POSTGRES_DB=n8n_database

# n8n Database Connection Details
DB_TYPE=postgres
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# n8n Access and Security
# -----------------------------------------------------------------------------

# PUBLIC DOMAIN: MUST match the domain Caddy is configured for (e.g., https://n8n.example.com)
# Set this as an environment variable before running the deploy script:
# export DOMAIN_NAME=n8n.example.com
WEBHOOK_URL=https://n8n.example.com

# OPTIONAL: Basic Auth for the n8n UI (recommended)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=another_secure_password_abcde

# General n8n Configuration
GENERIC_TIMEZONE=Europe/Berlin
